<html>
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=IE">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">

		<!-- Estilos -->
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet">
		<link rel="stylesheet" type="text/css" href="{{asset('css/jquery-ui.css')}}">
		<link rel="stylesheet" href="{{asset('css/jquery-ui.structure.css')}}">
		<link rel="stylesheet" href="{{asset('css/jquery-ui.theme.css')}}">
		<link rel="stylesheet" href="{{asset('css/landing.css')}}">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
		<link href="https://fonts.googleapis.com/css?family=Titillium+Web" rel="stylesheet">

		<!-- Scripts -->
		<script src="{{asset('js/jquery-3.7.1.js')}}"></script>
		<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"></script>
		<script src="{{asset('js/jquery-ui.js')}}"></script>
		<script src="{{asset('js/main.js')}}"></script>
        <script src="{{asset('js/pasarlista.js')}}"></script>
        <style>
        #cuerpo {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
        }
        #titulo {
            text-align: center;
            color: #333;
        }

        #lista {
            list-style-type: none;
            padding: 0;
        }

        #lista li {
            background-color: #fff;
            border: 1px solid #ddd;
            margin: 5px 0;
            padding: 10px;
            border-radius: 5px;
            display: flex;
        }

        .asistencia {
            color: #4caf50;
        }

        .ausencia {
            color: #f44336;
        }

        @media screen and (min-width: 600px) {
        #lista li span {
            width: 10%; /* Two columns on medium screens */
            margin-bottom: 0;
        }
    }

    @media screen and (max-width: 768px) {
        #lista li span {
            width: 30%; /* Three columns on larger screens */
        }
		.listado {
			text-align: left;
			overflow: hidden;
			width: 100%;
			margin: 0 auto;
			display: table;
            margin-top: 10%;
			padding: 0 2%;
		}
    }
    body {
        font-family: Helvetica Neue, Arial, sans-serif;
        font-size: 14px;
        color: #444;
    }

    @charset "UTF-8";
		@import 'https://fonts.googleapis.com/css?family=Raleway';
		body {
			font-family: 'Raleway', sans-serif;
			font-weight: 300;
			line-height: 1.42em;
			color:#4B4A4B;
			background-color:#FFDAC4;
			background: url(http://www.pixeden.com/media/k2/galleries/131/002-subtle-light-pattern-background-texture.jpg);
		}

		h1 {
			font-size:3em; 
			font-weight: 300;
			line-height:1em;
			text-align: center;
			color: #FD2E4A;
		}

		.listado th h1 {
			font-weight: bold;
			font-size: 1em;
			text-align: left;
		  	color: #FFFFFF;
		}

		.listado td {
			font-weight: normal;
			font-size: 1em;
			-webkit-box-shadow: 0 2px 2px -2px #0E1119;
			-moz-box-shadow: 0 2px 2px -2px #0E1119;
			box-shadow: 0 2px 2px -2px #0E1119;
		}

		.listado {
			text-align: left;
			overflow: hidden;
			width: 100%;
			margin: 0 auto;
			display: table;
            margin-top: 7%;
			padding: 0 2%;
		}

		.listado td, .listado th {
			padding-bottom: 2%;
			padding-top: 2%;
			padding-left:2%;  
		}

		.listado tr:nth-child(odd) {
			background-color:#c6c4c3;
		}

		.listado tr:nth-child(even) {
			background-color: #FFFFFF;
		}

		.listado th {
			background-color: #350133;
		}

		.listado td:first-child { 
			color: black; 
		}

		.listado tr:hover {
			-webkit-box-shadow: 0 6px 6px -6px #0E1119;
			-moz-box-shadow: 0 6px 6px -6px #0E1119;
			box-shadow: 0 6px 6px -6px #0E1119;
		}

		td a {
			color: #4B4A4B;
		}

		td a:hover {
			color: #FFFFFF;
		}
        td{
            text-align: center;
			border: 1px solid black;
        }

		.listado td.icono:hover {
			
			box-shadow: #BA0F26 -1px 1px, #BA0F26 -2px 2px, #BA0F26 -3px 3px, #BA0F26 -4px 4px, #BA0F26 -5px 5px, #BA0F26 -6px 6px;
			transform: translate3d(6px, -6px, 0);

			transition-delay: 0s;
			transition-duration: 0.4s;
			transition-property: all;
			transition-timing-function: line;
		}	

    </style>
    </head>
    <body id="cuerpo">
    {% block body %}
        {% include "header.html.twig" %}
        <h1 id="titulo" style="margin-top: 5%;">Lista de Estudiantes</h1>

        <table border="1" class="listado">
			<thead>
				<tr>
					<th><h1>Nombre</h1></th>
					<th><h1>Número personas</h1></th>
					<th><h1>Personas asistidas</h1></th>
					<th colspan="2"><h1>Accion</h1></th>
				</tr>
			</thead>
			<tbody>
                {% for reserva in tour.Reserva %}
                    <tr>
                        <td>{{reserva.Usuario.username}}</td>
                        <td class="num_personas">{{ reserva.getNumPersonas }}</td>
                        <td><input type="number" value="{{ reserva.getNumPersonas }}" class="form-control"></td>
                        <td class="si"><span class="asistencia ml-2">&#10004;</span></td>	
                        <td class="no"><span class="ausencia ml-2">&#10008;</span></td>	
                    </tr>
                {% endfor %}
			</tbody>
		</table>
        <input type="button" class="btn btn-primary" id="enviar" value="ENVIAR" style="margin-left: 46%; margin-top: 5%;">
    {% endblock %}
</body>
</html>